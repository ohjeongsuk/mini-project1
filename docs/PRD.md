# PRD — 머니로그(MoneyLog)

> **버전** 1.1 · **최종 수정** 2026-09-16
> 이 문서는 "무엇을 만드는가"를 정의한다. "어떻게 만드는가"는 `CLAUDE.md`, "언제 만드는가"는 `ROADMAP.md`를 참조한다.
>
> **완료 판정 기준은 `ROADMAP.md`의 검증 체크리스트가 정본이다.** 이 문서는 요구사항 정의만 담는다.

---

## 1. 배경과 목표

### 배경

가계부를 쓰겠다고 마음먹은 사람이 실제로 마주치는 선택지는 둘이다.

- **수기 또는 엑셀** — 자유롭지만 매번 표를 만들고 수식을 고쳐야 하고, 달이 바뀌면 시트를 복사한다. 몇 달 지나면 열이 어긋나 있다.
- **기존 가계부 앱** — 입력은 편하지만 계좌 연동 권한을 요구하거나, 광고와 부가 기능이 많아 "얼마 썼는지"를 확인하기까지 화면을 여러 번 거친다.

그리고 두 선택지 모두 **기록만 남기고 끝난다.** 석 달치 데이터가 쌓여 있어도 "이번 달 이 속도면 얼마 쓸까"에 답해주지 않는다. 사용자는 계산을 직접 해야 하고, 대부분은 하지 않는다.

기록의 마찰을 최소로 줄이면서, **쌓인 데이터가 스스로 답을 내놓는** 개인용 가계부에 공백이 있다.

### 목표

1. **수입·지출 한 건을 3초 안에** 기록할 수 있게 한다. 페이지 이동 없이 목록 화면에서 바로 입력한다.
2. 쌓인 데이터로 **이번 달 예상 지출을 자동 계산**해 보여준다. 사용자가 아무것도 설정하지 않아도 동작한다.
3. **고정지출(구독·월세 등)을 사용자가 등록하지 않아도 데이터에서 찾아낸다.**
4. 기존 엑셀 가계부를 **CSV로 그대로 들여오고, 언제든 되가져갈 수 있게** 한다.
5. 웹/모바일 반응형으로 브라우저 하나만 있으면 쓸 수 있게 한다.

### 비목표 (이번 범위 아님)

- **배포** — 로컬 개발 완료까지가 이번 범위다. 배포 여부와 방식은 미정
- 구글 등 소셜 로그인
- 오픈뱅킹·카드사 계좌 연동, 영수증 OCR
- 반복 거래 자동 등록 (「고정지출 자동 감지」가 같은 문제를 푼다)
- **CSV 가져오기 중복 검사** — 같은 파일을 두 번 올리면 거래가 두 번 등록된다. 가져오기가 1회성(엑셀 이전)이고 4.4가 「실패분만 골라 다시 올림」이라는 부분 재업로드를 전제하므로, 해시 기반 중복 제거 대신 화면 안내로 처리한다
- 다중 통화, 환율
- 자산/부채 관리, 순자산 추적, 투자 기록
- 가족·커플 공유 가계부
- 알림/푸시
- 모바일 네이티브 앱
- 다크 모드 수동 토글 (OS 설정 추종까지만)

---

## 2. 대상 사용자

| 구분 | 설명 |
|---|---|
| 주 사용자 | 혼자 생활비를 관리하는 20~40대. 엑셀 가계부를 써봤거나 가계부 앱을 쓰다 그만둔 경험이 있다 |
| 사용 환경 | 입력은 모바일 브라우저, 확인·분석은 데스크톱 브라우저 |
| 기술 수준 | 웹 서비스 사용에 익숙. 엑셀을 다뤄본 적이 있다 |

**핵심 시나리오**

> "점심을 먹고 나오면서 휴대폰으로 금액만 치고 '식비' 버튼을 눌러 저장한다. 3초. 주말에 노트북으로 대시보드를 열어 이번 달 이 속도면 얼마를 쓰게 될지, 어느 카테고리가 평소보다 튀었는지 확인한다."

**이탈 지점에 대한 전제**

가계부 앱이 실패하는 지점은 기능 부족이 아니라 **입력 귀찮음**이다. 사용자는 사흘 쓰다 멈추고, 데이터가 없으니 분석 기능도 무의미해진다. 따라서 이 제품에서 **가장 중요한 화면은 대시보드가 아니라 입력 폼**이며, 요구사항 우선순위도 그 기준으로 정한다.

---

## 3. 기능 요구사항

**우선순위 정의** — **P0**: MVP 필수(전부 구현해야 완료) · **P2**: 범위 밖 기록

> **P1(있으면 좋음)은 쓰지 않는다.** MVP 범위에서 "있으면 좋은" 항목은 결국 만들지 않으면서 완료 판정만 흐린다. 그런 항목은 9장 「향후 확장 후보」로 옮긴다.

> 아래 P0 항목은 `ROADMAP.md`의 Phase 작업과 1:1로 대응한다. P0를 빼고 완료 처리하지 않는다.

### 3.1 인증 (AUTH)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| AUTH-01 | 이메일과 비밀번호로 회원가입할 수 있다. 이메일이 곧 로그인 ID다. | P0 |
| AUTH-02 | 비밀번호는 **6자 이상이며 UTF-8 기준 72바이트 이하**여야 하고, BCrypt로 해싱해 저장한다. 한계를 넘으면 500이 아니라 **400 안내**를 받는다. | P0 |
| AUTH-03 | 이미 가입된 이메일로 가입 시 중복 안내를 받는다. | P0 |
| AUTH-04 | 이메일과 비밀번호로 로그인하면 24시간 유효한 JWT를 받는다. | P0 |
| AUTH-05 | **가입하면 기본 카테고리 9개가 자동으로 생성되어, 첫 거래를 바로 입력할 수 있다.** | P0 |
| AUTH-06 | 로그아웃하면 토큰이 제거되고 로그인 화면으로 이동한다. **서버 API 없이 클라이언트에서 토큰 삭제 + 캐시 초기화로 처리한다.** | P0 |
| AUTH-07 | 토큰이 없거나 **만료된** 상태로 보호된 화면에 접근하면 로그인 화면으로 이동한다. | P0 |
| AUTH-08 | 로그인 상태에서 내 닉네임을 공통 헤더에서 확인할 수 있다. 이메일은 `/auth/me` 응답에 포함되지만 **화면에는 표시하지 않는다**. | P0 |
| AUTH-09 | 구글 계정으로 로그인/가입한다. 같은 이메일의 로컬 계정이 있으면 거부한다. | P0 |

**제외**: 비밀번호 재설정, 이메일 인증, Refresh Token, 회원 탈퇴, 소셜 로그인

### 3.2 카테고리 (CAT)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| CAT-01 | 수입/지출 카테고리를 만들 수 있다. 이름(1~30자)과 색을 지정한다. | P0 |
| CAT-02 | 카테고리 이름·색·표시 순서를 수정할 수 있다. **구분(수입/지출)은 생성 후 바꿀 수 없다.** | P0 |
| CAT-03 | 카테고리를 삭제할 수 있다. 삭제해도 **그 카테고리를 쓰던 과거 거래는 목록과 집계에서 그대로 보인다**(이름 옆에 "삭제됨" 표시). | P0 |
| CAT-04 | 같은 구분 안에서 이름이 중복되면 안내를 받는다. **삭제한 이름은 다시 쓸 수 있다.** | P0 |
| CAT-05 | 다른 사용자의 카테고리는 조회·수정·삭제할 수 없다. | P0 |

### 3.3 거래 기록 (TXN)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| TXN-01 | **목록 화면을 떠나지 않고** 구분·금액·카테고리·날짜·거래처·메모를 입력해 거래를 등록할 수 있다. 날짜 기본값은 오늘이다. | P0 |
| TXN-02 | 저장하면 목록 맨 위에 즉시 나타나고, **폼은 비워지되 날짜와 구분은 유지**되어 연속 입력이 가능하다. | P0 |
| TXN-03 | **최근 사용한 카테고리 3개가 버튼으로 노출**되어 한 번에 선택할 수 있다. | P0 |
| TXN-04 | 금액은 **천단위 콤마가 자동으로 표시**되며, 0보다 커야 한다. 모바일에서 숫자 키패드가 뜬다. | P0 |
| TXN-05 | 거래의 구분과 카테고리의 구분이 어긋나면 저장되지 않고 안내를 받는다. | P0 |
| TXN-06 | 내 거래 목록을 **날짜 내림차순**으로 조회할 수 있다. 같은 날짜 거래가 여러 건이어도 페이지를 넘길 때 **중복되거나 누락되지 않는다.** | P0 |
| TXN-07 | 목록은 페이지당 20건씩 페이지네이션되고, **기간·구분·카테고리로 필터**할 수 있다. | P0 |
| TXN-08 | **거래처와 메모를 키워드로 검색**할 수 있다(대소문자 무시). | P0 |
| TXN-09 | 거래를 눌러 상세 화면에서 모든 항목을 수정하고 "저장" 버튼으로 반영한다. 자동 저장은 하지 않으며, **저장하지 않고 이탈하려 하면 확인을 받는다.** | P0 |
| TXN-10 | 거래를 삭제할 수 있으며, **클릭 즉시 목록에서 사라진다**. 실제로는 Soft Delete로 처리된다. | P0 |
| TXN-11 | 서버 요청이 실패하면 UI를 이전 상태로 되돌리고 실패를 알린다. | P0 |
| TXN-12 | 다른 사용자의 거래는 조회·수정·삭제할 수 없다. | P0 |

### 3.4 집계 · 예측 (STAT)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| STAT-01 | 대시보드에서 **선택한 달의 총수입·총지출·잔액**을 확인할 수 있다. 달을 앞뒤로 이동할 수 있다. | P0 |
| STAT-02 | **카테고리별 지출 비중**을 금액·비율과 함께 확인할 수 있다. | P0 |
| STAT-03 | **일별 지출 강도를 달력 형태(히트맵)**로 확인할 수 있다. | P0 |
| STAT-04 | **이번 달 예상 지출**을 확인할 수 있다. `확정 지출 + 평소 일평균 × 남은 일수`로 계산하며, 계산 근거(기준 개월 수, 경과 일수)를 함께 보여준다. | P0 |
| STAT-05 | **평소보다 30% 이상 튄 카테고리**를 이상치로 안내받는다. 월초(경과 7일 미만)에는 안내하지 않는다. | P0 |
| STAT-06 | **고정지출로 보이는 거래**(같은 거래처·비슷한 금액이 3개월 연속)를 자동으로 찾아 목록으로 보여준다. 자동 등록하지는 않는다. | P0 |
| STAT-07 | 데이터가 부족하면(직전 3개월에 거래 없음) 예측 대신 **"데이터가 조금 더 필요해요"**를 안내한다. | P0 |
| STAT-08 | 거래가 없는 달을 조회해도 오류 없이 **모두 0인 정상 화면**을 보여준다. | P0 |
| STAT-09 | LLM 기반 자연어 소비 리포트·절약 제안. | P2 |

> **STAT-04·05의 계산 수식은 `CLAUDE.md` 5장이 정본이다.** 이 문서와 화면 문구는 그 수식을 따른다. 프론트에서 다시 계산하지 않는다.

### 3.5 예산 (BUD)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| BUD-01 | 지출 카테고리별로 **해당 월의 예산 금액**을 설정할 수 있다. 한 화면에서 여러 카테고리를 채워 한 번에 저장한다. | P0 |
| BUD-02 | 예산을 비우거나 0으로 두면 **미설정 상태**가 된다. | P0 |
| BUD-03 | 대시보드에서 **카테고리별 소진율(사용액/예산)**을 막대로 확인할 수 있다. | P0 |
| BUD-04 | 예산을 **초과한 카테고리는 시각적으로 구분**되어 표시된다. | P0 |
| BUD-05 | 예산 초과 시 이메일/푸시 알림. | P2 |

### 3.6 데이터 가져오기·내보내기 (CSV)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| CSV-01 | 기간을 지정해 거래 내역을 **CSV 파일로 내려받을 수 있다.** | P0 |
| CSV-02 | 내려받은 CSV를 **Microsoft Excel에서 열었을 때 한글이 깨지지 않는다.** | P0 |
| CSV-03 | CSV 파일을 올려 거래를 **일괄 등록할 수 있다.** | P0 |
| CSV-04 | 가져오기 결과로 **성공 건수·실패 건수·실패 사유(행 번호 포함)**를 확인한다. 일부 행이 실패해도 나머지는 등록된다. | P0 |
| CSV-05 | 내보낸 파일을 **그대로 다시 가져오면 동일한 내역이 복원**된다(왕복 일치). 메모에 콤마·따옴표가 있어도 깨지지 않는다. | P0 |
| CSV-06 | CSV에 없는 카테고리 이름이 있으면 **그 행만 실패 처리**한다. 카테고리를 자동 생성하지 않는다. | P0 |
| CSV-07 | **엑셀에서 편집해 저장한 CSV도 그대로 가져올 수 있다.** CP949로 저장된 파일의 한글이 깨지지 않고, 금액의 천단위 콤마(`12,500`)와 `2026.09.14` 형식의 날짜도 인식한다. | P0 |

> **CSV-07은 `CSV-05`(왕복 일치)의 현실판이다.** 사용자는 내려받은 파일을 그대로 다시 올리지 않는다. **엑셀에서 열어 편집한 뒤** 올린다. 그 사이에 엑셀이 인코딩·숫자 서식·날짜 구분자를 바꾼다.

### 3.7 화면 상태 (UX)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| UX-01 | 데이터 로딩 중에는 스켈레톤 UI를 보여준다. | P0 |
| UX-02 | 거래가 하나도 없으면 안내 문구를 보여주고 입력 폼으로 유도한다. | P0 |
| UX-03 | 검색/필터 결과가 없으면 빈 상태와 구분되는 문구를 보여준다. | P0 |
| UX-04 | 요청 실패 시 **동작하는 재시도 버튼**이 있는 에러 상태를 보여준다. | P0 |
| UX-05 | 모바일(360px)부터 데스크톱(1920px)까지 레이아웃이 깨지지 않는다. | P0 |
| UX-06 | 모든 폼 요소에 label이 연결되어 있고, **거래 입력을 키보드만으로 완주**할 수 있다. | P0 |
| UX-07 | 디자인 토큰을 라이트/다크 양쪽으로 정의하고 OS 설정(`prefers-color-scheme`)을 따른다. | P0 |
| UX-08 | 목록의 금액이 **자릿수 기준으로 세로 정렬**되어 읽힌다(`tabular-nums`). | P0 |
| UX-09 | 사용자가 직접 테마를 전환하는 토글 UI. | P2 |

---

## 4. 사용자 플로우

### 4.1 신규 가입 → 첫 거래 등록
```
/signup → 이메일·비밀번호·닉네임 입력 → 가입 성공 (JWT 발급 + 기본 카테고리 9개 생성)
  → /dashboard (데이터 없음 안내)
  → /transactions → 퀵 입력 바에 금액 입력 → "식비" 버튼 클릭 → 저장
  → 목록 맨 위에 항목 표시, 폼은 비워지고 날짜·구분 유지
```

### 4.2 일상 사용 (모바일)
```
/transactions 진입 → 금액 12500 입력 (자동으로 "12,500" 표시)
  → 최근 카테고리 버튼 3개 중 "식비" 탭 → 저장
  → 3초 안에 완료. 화면 이동 없음
```

### 4.3 주말 점검 (데스크톱)
```
/dashboard 진입 (이번 달)
  → 총수입·총지출·잔액 카드
  → "이번 달 예상 지출 2,610,000원" (15일 경과 / 최근 3개월 기준)
  → "식비가 평소보다 34% 높아요" 이상치 안내
  → 카테고리 도넛 + 예산 소진율 막대 + 일별 히트맵
  → ◀ 버튼으로 지난달과 비교
```

### 4.4 엑셀 가계부 이전
```
/data → "CSV 가져오기" → 파일 선택 → 업로드
  → 결과: "42건 등록, 3건 실패"
  → 실패 사유: "7행: 카테고리 '식대'를 찾을 수 없습니다."
  → /settings/categories 에서 '식대' 추가 후 실패분만 다시 올림
```

### 4.5 고정지출 확인
```
/dashboard → "고정지출로 보이는 항목" 카드
  → 넷플릭스 17,000원 (3개월 연속, 최근 9/5)
  → 통신비 45,000원 (3개월 연속, 최근 9/12)
  → 합계 62,000원이 매달 빠져나간다는 사실을 등록 없이 확인
```

---

## 5. 화면별 요구사항

### 5.0 요구사항 ID ↔ 화면 매핑

> **이 표가 3장과 5장의 정합성 기준이다.** 3장에 요구사항을 추가하면 이 표에 행을 먼저 추가하고, 대응 화면(또는 「전역」)에 실제로 기술한다. 표에 없는 ID가 3장에 있으면 그 요구사항은 구현되지 않는다.

| ID | 구현 위치 |
|---|---|
| AUTH-01, AUTH-02, AUTH-03, AUTH-05 | `/signup` |
| AUTH-04 | `/login` |
| AUTH-06, AUTH-08 | 전역 (공통 헤더) |
| AUTH-07 | 전역 (라우트 보호) |
| CAT-01 ~ CAT-04 | `/settings/categories` |
| CAT-05 | 전역 (소유권 검증) |
| TXN-01 ~ TXN-05 | `/transactions` (퀵 입력 바) |
| TXN-06, TXN-07, TXN-08 | `/transactions` |
| TXN-09 | `/transactions/[id]` |
| TXN-10 | `/transactions`, `/transactions/[id]` |
| TXN-11 | `/transactions`, `/transactions/[id]` |
| TXN-12 | 전역 (소유권 검증) + `/transactions/[id]` (404 화면) |
| STAT-01 ~ STAT-08 | `/dashboard` |
| BUD-01, BUD-02 | `/budgets` |
| BUD-03, BUD-04 | `/dashboard` |
| CSV-01 ~ CSV-07 | `/data` |
| UX-01, UX-04 | `/dashboard`, `/transactions`, `/transactions/[id]` |
| UX-02, UX-03 | `/transactions` |
| UX-05, UX-06, UX-07, UX-08 | 전역 |
| AUTH-09 | `/login`, `/signup` (구글 버튼) + `/oauth/callback` |
| STAT-09, BUD-05, UX-09 | — (P2, 범위 밖) |

---

### 5.1 전역 적용 요구사항 (모든 화면 공통)

> 특정 화면 하나에 속하지 않는 요구사항이다. 화면별 절에 중복 기술하지 않고 여기에만 둔다.

**공통 헤더** (인증이 필요한 모든 화면)
- 닉네임 표시, 네비게이션(대시보드 · 내역 · 예산 · 설정), 로그아웃 버튼 — `AUTH-06`, `AUTH-08`
- 로그아웃: 토큰 삭제 + React Query 캐시 초기화 → `/login` 이동. **서버 API 호출 없음**
- 모바일에서는 네비게이션을 하단 탭 바로 전환한다

**라우트 보호** — `AUTH-07`
- 토큰이 없거나 **만료된** 상태로 보호된 화면 접근 시 `/login`으로 이동
- 토큰 문자열의 존재 여부가 아니라 **`exp` 클레임을 디코드해 판정**한다. 만료 토큰이 판정을 통과하면 보호 화면이 401 왕복 동안 노출된다
- 인증 판정이 끝나기 전에는 스켈레톤을 보여준다
- API 응답이 401이면 자동 로그아웃 처리

**소유권 · 404** — `TXN-12`, `CAT-05`
- 타인 소유이거나 존재하지 않는 리소스는 서버가 404로 응답한다(존재 여부를 노출하지 않음)
- 화면은 목록으로 자동 리다이렉트하지 않고 "찾을 수 없습니다" 상태를 보여준다

**금액 표시 · 입력** — `TXN-04`, `UX-08`
- 표시: 천단위 콤마 + `tabular-nums`. 수입은 초록(`#10B981`), 지출은 빨강(`#EF4444`)
- 입력: `<input type="number">`를 쓰지 않는다. 콤마가 들어가면 값이 사라지기 때문이다 (`CLAUDE.md` 8장)
- 포맷·파싱은 `lib/money.ts` 한 곳에서만 수행한다

**날짜 기준** — `STAT-01`, `STAT-04`
- 조회 대상 월(`yearMonth`)과 사용자의 오늘(`asOf`)은 **클라이언트가 서버로 보낸다.** 서버가 `now()`로 추론하지 않는다
- 서버는 UTC로 돌기 때문에, 서버가 판정하면 매월 1일 0~9시에 사용자가 지난달 대시보드를 보게 된다 (`CLAUDE.md` 4장)

**반응형** — `UX-05`
- 360px ~ 1920px에서 레이아웃이 깨지지 않는다
- 컨테이너 `max-w-5xl`, 패딩 모바일 16px · 데스크톱 24px
- 퀵 입력 바는 데스크톱에서 한 줄, 모바일에서 세로로 쌓인다

**접근성** — `UX-06`
- 모든 입력에 label이 연결되어 있다
- **거래 입력을 키보드만으로 완주할 수 있다.** Tab 순서가 금액 → 카테고리 → 날짜 → 저장으로 이어지고, Enter로 저장된다

**다크 모드** — `UX-07`
- 디자인 토큰을 라이트/다크 양쪽으로 정의하고 `prefers-color-scheme`을 따른다. 토글 UI는 두지 않는다
- 카테고리 색은 사용자가 지정한 값이므로 다크 모드에서도 그대로 쓴다. 대신 **텍스트는 색 위에 올리지 않고 색 옆에 둔다**(대비 계산이 불가능하기 때문)

**에러 문구 매핑** (서버 `error.code` → 사용자에게 보이는 문구)

| 코드 | 화면 문구 | 표시 방식 |
|---|---|---|
| `INVALID_INPUT` | 서버가 준 필드별 메시지 | 해당 입력 아래 인라인 |
| `UNAUTHORIZED` (로그인 시) | "이메일 또는 비밀번호가 올바르지 않습니다." | 폼 상단 인라인 |
| `UNAUTHORIZED` (그 외) | 문구 없이 `/login`으로 이동 | — |
| `EMAIL_DUPLICATED` | "이미 사용 중인 이메일입니다." | 이메일 입력 아래 인라인 |
| `CATEGORY_DUPLICATED` | "같은 이름의 카테고리가 이미 있습니다." | 이름 입력 아래 인라인 |
| `CATEGORY_TYPE_MISMATCH` | "수입/지출 구분이 카테고리와 맞지 않습니다." | 폼 인라인 |
| `TRANSACTION_NOT_FOUND` | "거래 내역을 찾을 수 없습니다." | 전체 화면 상태 |
| `CATEGORY_NOT_FOUND` | "카테고리를 찾을 수 없습니다." | 토스트 |
| `INVALID_CSV` | "CSV 파일을 읽을 수 없습니다. 형식을 확인해 주세요." | 토스트 |
| `INTERNAL_ERROR` | "일시적인 오류가 발생했습니다. 다시 시도해 주세요." | 토스트 또는 에러 카드 |
| 네트워크 실패 | "연결에 실패했습니다." + 재시도 버튼 | 에러 카드 |

> 로그인 실패는 **계정 존재 여부를 구분해 노출하지 않는다.** 비밀번호 오류와 미가입 이메일에 같은 문구를 쓴다.

---

### 5.2 `/login`
- 이메일·비밀번호 입력, "로그인" 버튼 — `AUTH-04`
- 회원가입 링크
- 실패 시 인라인 에러 메시지 (5.1 에러 문구 매핑 적용)

### 5.3 `/signup`
- 이메일·비밀번호·닉네임 입력 — `AUTH-01`
- 비밀번호는 **제출 전에 6자 이상 / 72바이트 이하를 인라인으로 안내**한다 — `AUTH-02`
  - 한글은 1자가 3바이트임을 안내 문구에 포함한다
- 이메일 중복 시 인라인 안내 — `AUTH-03`
- 가입 성공 시 JWT 저장 + **기본 카테고리 9개가 서버에서 함께 생성**된 상태로 `/dashboard` 이동 — `AUTH-05`

### 5.4 `/dashboard`
**월 선택** — `STAT-01`
- `◀ 2026년 9월 ▶` 형태. 선택한 월은 URL 쿼리(`?ym=2026-09`)로 관리해 뒤로가기가 동작한다
- 미래 달로는 이동할 수 없다

**요약 카드** — `STAT-01`
- 총수입 / 총지출 / 잔액 3개 카드. 잔액이 음수면 빨강

**예측 카드** — `STAT-04`, `STAT-07`
- "이번 달 이 속도면 **2,610,000원**을 쓰게 돼요"
- 보조 문구: "15일 경과 · 최근 3개월 기준"
- **과거 달을 보고 있으면 예측 카드를 숨긴다.** 끝난 달의 "예상"은 의미가 없다
- 데이터 부족 시: "예측하려면 데이터가 조금 더 필요해요" — `STAT-07`

**이상치 안내** — `STAT-05`
- "식비가 평소보다 34% 높아요" 형태로 최대 3개
- 경과 7일 미만이거나 해당 없으면 카드를 렌더링하지 않는다

**고정지출 카드** — `STAT-06`
- 감지된 항목의 거래처·금액·연속 개월 수·최근 날짜, 그리고 합계
- **"등록" 버튼을 두지 않는다.** 이 카드는 읽기 전용 안내다
- 별도 API로 로드되므로 이 카드만 따로 스켈레톤을 보여준다

**차트** — `STAT-02`, `STAT-03`, `BUD-03`, `BUD-04`
- 카테고리 도넛 + 금액·비율 목록 — `STAT-02`
- 예산 소진율 막대. 초과 카테고리는 막대와 수치를 빨강으로 — `BUD-03`, `BUD-04`
- 일별 히트맵(달력 7열 격자). 지출액에 따라 배경 농도 4단계 — `STAT-03`
  - 1일의 요일에 맞춰 앞쪽 칸을 비운다
  - 각 칸에 날짜와 금액을 툴팁/보조 텍스트로 제공한다

**빈 상태** — `STAT-08`
- 거래가 없는 달은 에러가 아니라 모두 0인 정상 화면이다. 차트 자리에 "이 달에는 기록이 없어요"를 보여주고 월 이동 버튼은 그대로 동작한다

### 5.5 `/transactions`

**퀵 입력 바 (화면 최상단)** — `TXN-01` ~ `TXN-05`
- 필드: 구분 토글(수입/지출) · 금액 · 카테고리 select · 날짜(기본 오늘) · 거래처 · 메모
- **최근 사용 카테고리 3개를 버튼으로 노출** — `TXN-03`
  - 별도 API를 만들지 않고, 이미 받아온 목록 앞쪽에서 중복 제거해 뽑는다
- 저장 시 버튼에 로딩 상태 표시. 성공하면 목록 맨 위에 추가되고 **폼은 비워지되 날짜·구분은 유지** — `TXN-02`
- 카테고리가 하나도 없으면 폼을 비활성화하고 `/settings/categories` 링크를 안내한다
- **낙관적 업데이트를 하지 않는다.** 서버가 채우는 `id`·카테고리 조인 결과가 있어 임시 데이터와 어긋난다 (`CLAUDE.md` 9장)

**필터 · 검색** — `TXN-07`, `TXN-08`
- 기간(from/to) · 구분 · 카테고리 · 키워드. 전부 URL 쿼리로 관리
- 키워드는 거래처와 메모를 함께 검색한다

**목록** — `TXN-06`, `TXN-10`
- 날짜 내림차순, 페이지당 20건
- 각 행: 날짜 · 카테고리 배지(색) · 거래처 · 금액(수입 초록 / 지출 빨강) · 삭제 버튼
- 삭제는 **낙관적 업데이트**로 즉시 사라진다 — `TXN-10`
- 마지막 항목 삭제로 현재 페이지가 비면 이전 페이지로 이동한다(`page > 0`일 때)
- 삭제된 카테고리가 붙은 거래는 배지 옆에 "삭제됨" 표시 — `CAT-03`

**빈 상태 / 검색 결과 없음** — `UX-02`, `UX-03`
- 거래 없음: "아직 기록이 없어요" + 퀵 입력 바로 포커스 이동
- 검색 결과 없음: "조건에 맞는 내역이 없어요" + 필터 초기화 버튼

### 5.6 `/transactions/[id]`
- 진입 즉시 **모든 필드가 편집 가능한 상태**다. 보기/편집 모드를 나누지 않는다 — `TXN-09`
- `/transactions`의 퀵 입력 바와 **같은 폼 컴포넌트(`TransactionForm`)를 재사용**하고, 초기값 유무와 삭제 버튼 노출로만 구분한다
- 자동 저장 없이 "저장" 버튼으로 반영한다
- **변경 사항이 있는 상태로 이탈하려 하면 확인 대화상자**를 띄운다 — `TXN-09`
  - 새로고침·탭 닫기 / 화면 내 버튼 / 브라우저 뒤로가기 **세 경로를 모두 막는다** (`CLAUDE.md` 9장)
  - 금액의 `dirty` 판정은 **콤마를 제거한 원본 값끼리** 비교한다
- 저장 실패 시 폼 내용을 유지한 채 에러를 표시한다 — `TXN-11`
- 없는 id이거나 타인 소유이면 "거래 내역을 찾을 수 없습니다" 화면 + 목록으로 가기 버튼

### 5.7 `/budgets`
- 월 선택(대시보드와 동일 UI), **지출 카테고리 전체가 행으로 나열**되고 각 행에 금액 입력 — `BUD-01`
- 비우거나 0이면 미설정 — `BUD-02`
- "저장" 버튼 하나로 전체를 upsert한다. 개별 저장 버튼을 두지 않는다
- 저장 후 대시보드 소진율이 즉시 갱신되도록 `['stats']` 캐시를 함께 무효화한다

### 5.8 `/settings/categories`
- 수입/지출 탭으로 구분해 목록 표시 — `CAT-01`
- 각 행: 색 점 · 이름 · 표시 순서 · 수정/삭제 버튼
- 추가 폼: 이름 + 색 선택(§8 팔레트에서 고르거나 직접 입력) — `CAT-01`
- **구분(수입/지출)은 수정 폼에 두지 않는다** — `CAT-02`
- 이름 중복 시 인라인 안내 — `CAT-04`
- 삭제 시 "이 카테고리를 쓰는 과거 내역은 그대로 남습니다"를 확인 문구에 포함 — `CAT-03`

### 5.9 `/data`
**내보내기** — `CSV-01`, `CSV-02`
- 기간 선택(기본: 이번 달) → "CSV 내려받기"
- 파일명 `moneylog_2026-09.csv`
- **UTF-8 BOM을 포함해 Excel에서 한글이 깨지지 않는다** — `CSV-02`

**가져오기** — `CSV-03` ~ `CSV-07`
- 파일 선택 → 업로드 → 결과 표시
- 결과: "42건 등록, 3건 실패" + 실패 행 목록(행 번호 + 사유) — `CSV-04`
- 형식 안내를 화면에 표로 보여준다(헤더 6열, 구분은 `수입`/`지출`, 카테고리는 이름으로 매칭)
- 상한 안내: 1MB / 5,000행
- **엑셀이 되돌려준 파일도 읽는다** — CP949 인코딩, 금액의 천단위 콤마, `2026.09.14` 날짜를 모두 인식한다 — `CSV-07`
- **중복 안내**: "이미 가져온 파일을 다시 올리면 중복 등록됩니다" — 중복 검사를 하지 않는다(1장 비목표)
- 성공 시 `['transactions']`·`['stats']` 캐시를 무효화한다

---

## 6. 데이터 요구사항

**데이터 제약(필드 길이·형식·금액 타입·삭제 정책·정렬·검색 규칙)은 `CLAUDE.md` 4장이 정본이다.** 같은 제약값을 두 곳에 적으면 한쪽만 고쳐져 갈라지므로 여기에 다시 적지 않는다.

---

## 7. 비기능 요구사항

| 항목 | 기준 | 검증 위치 |
|---|---|---|
| 입력 속도 | **거래 1건 등록이 화면 이동 없이 3회 조작(금액 입력 → 카테고리 → 저장) 이내** | Phase 9 |
| 응답 속도 (목록) | **키워드 검색 포함** 목록 조회 500ms 이내 (로컬, **시드 20,000건**, 워밍업 후 중앙값) | Phase 4 |
| 응답 속도 (집계) | 월 대시보드 집계 **1초 이내** (로컬, 시드 20,000건, 워밍업 후 중앙값) | Phase 5 |
| 금액 정확성 | **합계·평균·소진율 계산에 부동소수점 오차가 없다.** `BigDecimal` 사용, 나눗셈은 scale·RoundingMode 명시 | Phase 5 |
| 시각 정확성 | **집계·예측 코드에 서버 `now()` 호출이 없다.** 기준 날짜는 클라이언트가 전달 | Phase 5 |
| 체감 반응 | 삭제는 낙관적 업데이트로 즉시 반영 | Phase 11 |
| 브라우저 | **Chromium 계열 1종 + 다른 엔진 1종** (Windows: Chrome + Edge/Firefox) | Phase 12 |
| 반응형 | 360px ~ 1920px (Phase 9는 360px, Phase 12는 상한까지) | Phase 9·12 |
| 인증 보안 | JWT 인증, 소유권 검증, 입력값 검증 | Phase 3·4 |
| 시크릿 관리 | 환경변수로 분리, 저장소에 커밋하지 않음 | Phase 1·12 |
| CSV 호환 | **내보낸 파일이 Microsoft Excel에서 한글 깨짐 없이 열린다** | Phase 6 |
| 문서화 | 모든 API가 Swagger UI에 노출 | Phase 4·5·6 |
| 접근성 | 폼 label 연결, 거래 입력 키보드 완주 | Phase 8·9 |

> 검증 위치가 없는 요구사항은 검증되지 않는다. 새 요구사항을 추가할 때 반드시 Phase를 함께 지정한다.

---

## 8. 완료 판정

MVP 완료 여부는 **`ROADMAP.md` Phase 12의 검증 체크리스트**로 판정한다. 이 문서에는 별도의 성공 기준을 두지 않는다. (두 곳에 두면 반드시 갈라지기 때문)

---

## 9. 향후 확장 후보 (범위 밖)

우선순위 순으로 기록만 해둔다. 이번 MVP에서는 구현하지 않는다.

1. **배포** — 방식 미정. AWS 조합(Amplify + EC2 + RDS)을 전제로 제약만 지켜둔 상태
2. **LLM 소비 리포트** (`STAT-09`) — 집계 결과를 Claude API에 넘겨 자연어 리포트·절약 제안 생성
3. **정산 주기(급여일 기준 집계)** — 집계 기준을 달력 월에서 `[매월 D일 ~ 다음달 D-1일]`로 바꾼다. 급여일이 25일인 사용자에게 달력 월 집계는 생활 주기와 어긋난다.
   > **지금 도입 비용이 낮은 구조라는 점을 기록해둔다.** 기간을 파생값으로 두는 것(=`transactions`에 기간 키를 저장하지 않음)이 이 기능의 전제인데, 우리는 `txn_date`만 갖고 있어 이미 충족한다. `budgets.year_month CHAR(7)`도 그대로 기간 키로 쓸 수 있다. 바뀌는 것은 **조회 범위 계산과 API 파라미터뿐**이고 스키마·인덱스는 유지된다. 다만 모든 집계·예산 쿼리가 이 기준을 타므로, 도입 시점에는 `/stats`·`/budgets` 전부를 함께 고쳐야 한다.
4. **삭제 취소(휴지통)** — 거래·카테고리를 Soft Delete하면서 복원 경로가 없다. 데이터는 이미 `deleted_at`으로 남아 있으므로 목록 필터 + 복원 API만 얹으면 된다
5. **영수증 사진 첨부** — 로컬 스토리지로 먼저 완성 후 S3 전환
6. 반복 거래 자동 등록 (고정지출 감지를 "등록"으로 승격)
7. 예산 초과 알림 (`BUD-05`)
8. 결제수단(카드/현금/계좌) 분류, 태그
9. **거래 분할(Split)** — 한 거래를 카테고리별 하위 항목으로 쪼갠다. ATM 출금 50,000원에 점심 12,000·택시 8,000을 기입하면 미기입 30,000원이 원래 카테고리에 남는 식이다. 현금 지출을 다루는 실용적인 모델이며, **하위 항목에 날짜를 두지 않고 부모 거래일에 귀속**시키면 `총지출 = 카테고리 합계`가 항상 성립한다
10. 연간 리포트, 전년 동월 비교
11. 다크 모드 수동 토글 (`UX-09`)
12. 카테고리 계층 구조 (대분류/소분류)
13. 자산/부채 및 순자산 추적
